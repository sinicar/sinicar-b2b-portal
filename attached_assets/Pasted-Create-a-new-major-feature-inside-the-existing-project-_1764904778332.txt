Create a new major feature inside the existing project:

SINI CAR – B2B Wholesale Customer Portal
(React + Vite + TypeScript + Tailwind + mockApi + LocalStorage + i18n AR/EN/HI/ZH)

GLOBAL RULES:

Read the full codebase first (pages, components, hooks, context, mockApi, types).

Do NOT delete, rewrite, or break any existing logic or design.

Everything added must be configurable from the Admin UI. No hard-coded behavior.

All data must use mockApi + LocalStorage as the backend.

Follow the exact coding style used across the project.

Build this feature on top of the current system without affecting Customers, Orders, Products, Tools, Suppliers, Affiliates, etc.

Your task: Implement a complete “Advertising Management System” inside the Admin Dashboard.

This includes two major parts:
(1) Creating data models + mockApi integration
(2) Creating new Admin UI pages for managing Advertisers, Campaigns, and Ad Slots

STEP 1 — CREATE MODELS (INTERFACES) AND CONNECT THEM TO mockApi

Create a new file such as:
src/models/advertising.ts
(or place these interfaces in the appropriate folder if the project already has a models/types directory)

Add the following models exactly:

Advertiser model:

export interface Advertiser {
id: string;
name: string;
contactName?: string;
phone?: string;
email?: string;
whatsapp?: string;
companyType?: 'supplier' | 'shipping' | 'workshop' | 'other';
source?: string;
status: 'active' | 'pending_verification' | 'suspended' | 'blacklisted';
notes?: string;
createdAt: string;
updatedAt: string;
}

AdCampaign model:

export interface AdCampaign {
id: string;
advertiserId: string;
name: string;
type: 'banner_top' | 'banner_sidebar' | 'card_in_tools' | 'card_in_products' | 'popup';
targetPages: string[];
priority: number;
startDate: string;
endDate?: string;
budgetType?: 'fixed' | 'per_view' | 'per_click';
maxViews?: number;
maxClicks?: number;
landingUrl?: string;
imageUrl?: string;
status: 'draft' | 'pending_approval' | 'running' | 'paused' | 'rejected' | 'ended';
createdAt: string;
updatedAt: string;
}

AdSlot model:

export interface AdSlot {
id: string;
slotKey: string;
nameAr: string;
nameEn: string;
descriptionAr?: string;
descriptionEn?: string;
isEnabled: boolean;
maxAds: number;
selectionMode: 'by_priority' | 'rotate' | 'random';
visibleForCustomerGroups?: string[];
visibleForSpecificCustomers?: string[];
createdAt: string;
updatedAt: string;
}

mockApi IMPLEMENTATION

Add CRUD functions for each entity.
Follow the same pattern used in the project for other modules.

For Advertisers (LocalStorage key: "advertisers"):

getAdvertisers()

getAdvertiserById(id)

createAdvertiser(data)

updateAdvertiser(id, data)

deleteAdvertiser(id)

For AdCampaigns (LocalStorage key: "adCampaigns"):

getAdCampaigns()

getAdCampaignById(id)

createAdCampaign(data)

updateAdCampaign(id, data)

deleteAdCampaign(id)

For AdSlots (LocalStorage key: "adSlots"):

getAdSlots()

getAdSlotByKey(slotKey)

createAdSlot(data)

updateAdSlot(id, data)

Add a helper:

getActiveAdsBySlot(slotKey: string, customerId?: string):

Read slot settings (AdSlot)

Filter campaigns:

status = running

startDate <= today

endDate empty or >= today

Apply selectionMode:

by_priority → sort by priority

rotate → rotate through items (store index in LocalStorage)

random → random selection

Limit results using slot.maxAds

Return matching ads

STEP 2 — CREATE ADMIN UI PAGE: “Advertising Management”

Add a new Admin route:
/admin/advertising

Add this page to the Admin sidebar menu:
Label: "Advertising & Advertisers"

This page contains FOUR tabs:

Advertisers

Ad Campaigns

Ad Slots / Placements

Reports

Use the same Tabs, Tables, Form Modals, and Layout styles already used across the system.

TAB 1 — ADVERTISERS

Top of the page:

Button: Add New Advertiser

Filters:

status

companyType

keyword search (name / phone / email)

Advertisers Table columns:

name

company type

contact info

status

createdAt

actions (view / edit)

Add a modal for:

Create Advertiser

Edit Advertiser

Fields:

name

contactName

phone

email

whatsapp

companyType

source

status

notes

TAB 2 — AD CAMPAIGNS

Top of the page:

Button: Create New Campaign

Filters:

advertiser

type

status

date range

Campaign Table columns:

campaign name

advertiser name

type

target pages

start/end dates

status

actions (view / edit / pause / end)

Campaign form fields:

advertiserId (dropdown)

name

type

targetPages (multi-select)

priority

startDate / endDate

budgetType + maxViews + maxClicks (optional)

landingUrl

imageUrl

status

TAB 3 — AD SLOTS (PLACEMENTS)

This section manages the locations where ads appear in the Customer Portal.

Slots Table columns:

name

slotKey

isEnabled (toggle)

maxAds

selectionMode

actions (edit)

Edit Slot modal:

nameAr / nameEn

descriptionAr / descriptionEn

isEnabled

maxAds

selectionMode

optional future: visibleForCustomerGroups, visibleForSpecificCustomers

If LocalStorage has no AdSlots, initialize default slots:

HOME_TOP_BANNER

CUSTOMER_DASHBOARD_SIDEBAR

TOOLS_PAGE_BANNER

PRODUCTS_PAGE_CARD

TAB 4 — REPORTS

Show simple metrics:

total advertisers

active advertisers

total campaigns

running campaigns

Use mockApi functions for these numbers.

PERMISSIONS

Add new permission keys:

canViewAdvertisers

canEditAdvertisers

canManageAdCampaigns

canManageAdSlots

Use them to show/hide actions in the UI.

i18n REQUIREMENTS

Every new text (labels, table headers, modal titles, buttons) must be added to the AR/EN translation files.
Do NOT hardcode English or Arabic strings inside components.

FINAL REQUIREMENTS

Follow existing file structure and coding style strictly.

No existing functionality should break.

Everything must be fully configurable via the Admin UI.

No hard-coded logic. Every behavior must be controlled through settings and LocalStorage.

Keep RTL support and Tailwind styling consistent with the current project.