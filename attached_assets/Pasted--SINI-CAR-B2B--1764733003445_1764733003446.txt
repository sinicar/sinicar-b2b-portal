هذا مشروع اسمه SINI CAR – نظام عملاء الجملة B2B مخصص لشركة صيني كار (قطع غيار سيارات صينية) لإدارة طلبات الجملة بين العملاء (التجّار) وبين الإدارة.

⚠️ مهم جدًا:
أريدك أن تعمل على هذا المشروع كما هو، لا تعيد بناءه من الصفر، لا تحذف ملفات، لا تلغي أنواع (Types)، لا تغيّر مفاتيح LocalStorage، ولا تستبدل منطق الـ mockApi.
المطلوب هو: تكملة المشروع، إصلاح الأخطاء، وربط لوحة العملاء بلوحة تحكم الأدمن، مع فحص كل الأزرار.

🧩 1) التقنيات المستخدمة (Tech Stack)

المشروع مبني على:

React + Vite

TypeScript

Tailwind CSS

نظام بيانات داخلي عبر:

ملف types.ts لتعريف جميع أنواع البيانات

ملف services/mockApi.ts لمحاكاة الـ Backend وتخزين كل شيء في localStorage

بعض الأدوات في utils/ مثل:

arabicSearch.ts للبحث العربي الذكي

partNumberUtils.ts لتوحيد و Normalization لأرقام القطع

dateUtils.ts لتنسيق التواريخ والـ Timeline

LanguageContext لإدارة اللغة (عربي/إنجليزي)

ToastContext و Toast للإشعارات الداخلية

ملاحظة مهمة:
كل هذا يجب أن يبقى كما هو، فقط نُكمّل فوقه.

🧱 2) بنية المشروع (Architecture Overview)

App.tsx

مسؤول عن:

شاشة تسجيل الدخول (Login)

تحديد نوع المستخدم: admin أو employee أو customer

عرض لوحة العميل (Dashboard العميل) أو لوحة الأدمن حسب الدور UserRole.

types.ts
يحتوي أنواع مهمة مثل:

User, UserRole, EmployeeRole

CustomerAccount, CustomerType

Product

QuoteRequest, QuoteItem

MissingPartRequest

ImportRequest

ActivityLogEntry

إعدادات عامة، صلاحيات، حالة حساب العميل، إلخ.

رجاءً لا تغيّر أسماء هذه الأنواع ولا الحقول الموجودة فيها. إذا احتجت تضيف، أضف بدون حذف أو تعديل جذري.

services/mockApi.ts

هذا هو قلب النظام الآن.

يحتوي كائن أو كلاس اسمه مثلاً MockApi يقوم بـ:

إدارة المستخدمين والحسابات (Customers / Employees / Admin)

إدارة المنتجات Products

إدارة عروض الأسعار QuoteRequests

إدارة طلبات النواقص MissingPartRequest

إدارة طلبات الاستيراد من الصين ImportRequest

إدارة السجل / التاريخ ActivityLogEntry

كل ذلك محفوظ في localStorage باستخدام مفاتيح ثابتة موجودة في STORAGE_KEYS.

🔴 مهم:
لا تغيّر مفاتيح STORAGE_KEYS أو أسماء الحقول في البيانات المخزنة.
الهدف أن تبقى البيانات القديمة متوافقة (Backward Compatible).

components/

ستجد فيه صفحات ومكونات مثل (الأسماء تقريبية حسب المشروع):

Dashboard.tsx
→ لوحة العميل (B2B Customer Dashboard)

AdminDashboard.tsx
→ لوحة تحكم الأدمن مع إحصائيات وملخصات

QuoteRequestPage.tsx / AdminQuoteManager.tsx
→ إدارة عروض الأسعار (من جهة العميل + جهة الأدمن)

OrdersPage.tsx / AdminOrdersManager.tsx
→ إدارة الطلبات (الطلبات المقبولة / المحجوزة / المكتملة…)

AdminMissingParts.tsx
→ شاشة الأدمن لطلبات (النواقص / المنتجات غير المتوفرة)

ImportFromChinaPage.tsx + AdminImportManager.tsx
→ شاشات طلبات الاستيراد من الصين (عميل + أدمن)

AdminCustomersPage.tsx
→ إدارة قاعدة العملاء (CRM)

OrganizationPage.tsx
→ إدارة الموظفين والأدوار والصلاحيات

AdminSettings.tsx
→ إعدادات عامة للنظام

NotificationBell.tsx
→ أيقونة الإشعارات للعميل/الأدمن مرتبطة بسجل النشاط

UsageIntroModal.tsx, Modal.tsx, Toast.tsx
→ واجهات مساعدة، نوافذ منبثقة، رسائل نجاح/خطأ

⚙️ 3) منطق العمل (Business Logic / Flow)
أ) من جهة العميل (Customer Flow)

العميل يسجل الدخول إلى لوحة العميل.

يستطيع أن:

يتصفح المنتجات (المستوردة من Excel أو المضافة يدويًا).

يطلب عرض سعر (Quote Request) لعدة قطع.

إذا لم يجد قطعة:

يقدم طلب منتج غير متوفر → يتحول إلى MissingPartRequest.

إذا كان طلب كبير أو خاص استيراد:

يقدم طلب استيراد من الصين → يتحول إلى ImportRequest.

كل عملية يقوم بها العميل (إرسال طلب، تعديل، إلغاء…)
يجب أن تُسجل في ActivityLogEntry وتظهر في الـ Timeline للأدمن والعميل.

ب) من جهة الأدمن (Admin Flow)

الأدمن يدخل لوحة التحكم.

يشاهد:

Dashboard الأدمن:

عدد العملاء النشطين

عدد عروض الأسعار المفتوحة/المغلقة

عدد طلبات النواقص

عدد طلبات الاستيراد

آخر نشاط (Activity Log)

صفحة عروض الأسعار:

كل QuoteRequest المرسلة من العملاء.

يمكنه تغيير حالة الطلب (جديد، تحت المراجعة، تم إرسال عرض سعر، مقبول، مرفوض…).

إضافة ملاحظات داخلية أو ملاحظات للعميل.

صفحة النواقص:

قائمة بكل MissingPartRequest مع إمكانية:

تغيير الحالة (قيد البحث، تم توفيرها، لا يمكن توفيرها…).

ربطها بعرض سعر أو طلب استيراد إذا لزم.

صفحة الاستيراد من الصين:

متابعة ImportRequest حسب الحالة:

قيد التسعير في الصين

في المصنع

تم حجز الشحن

في البحر

في الميناء

تم التخليص الجمركي

جاهز للاستلام

CRM / العملاء:

إدارة CustomerAccount

تفعيل/تعليق الحساب

رؤية تاريخ العميل (عروض أسعار، طلبات نواقص، استيراد…).

Organization / الموظفين:

الموظفين وأدوارهم EmployeeRole

تحديد الصلاحيات (من يمكنه رؤية ماذا)

أي حركة يقوم بها الأدمن أو الموظف (تغيير حالة، إضافة ملاحظة، تفعيل حساب…)
يجب أن تُسجل أيضًا في ActivityLogEntry وتظهر في Timeline.

🎯 4) المطلوب منك (مهامك بالتحديد)
أولًا: احترام الكود الحالي

لا تعيد إنشاء المشروع.

لا تحذف صفحات أو مكونات.

لا تغير تصميم الموقع إلا تغييرات بسيطة ضرورية (إصلاح تنسيق/محاذاة).

لا تغير أسماء الملفات أو أنواع TypeScript الموجودة في types.ts.

لا تغير مفاتيح التخزين في localStorage.

ثانيًا: ربط لوحة العميل بلوحة تحكم الأدمن

ربط عروض الأسعار

عند قيام العميل بإنشاء QuoteRequest من Dashboard أو QuoteRequestPage:

يجب أن تُحفظ في MockApi وتظهر مباشرة في شاشة الأدمن AdminQuoteManager / AdminDashboard.

عند تغيير الأدمن حالة عرض السعر:

يظهر التحديث عند العميل في صفحة عروض الأسعار الخاصة به (مثلاً "عرض السعر قيد المراجعة" → "تم إرسال عرض سعر" → "تم الموافقة").

ربط طلبات النواقص (Missing Parts)

عندما يرسل العميل طلب "منتج غير متوفر" من لوحة العميل:

يتسجل MissingPartRequest في MockApi.

يظهر في صفحة AdminMissingParts.

تغيير حالة الطلب من الأدمن يجب أن يظهر للعميل (مثلاً "قيد البحث" → "تم توفير القطعة").

ربط طلبات الاستيراد من الصين

عندما يقدم العميل طلب استيراد من لوحة ImportFromChinaPage:

تحفظ كـ ImportRequest في MockApi.

تظهر في AdminImportManager.

عند تعديل حالة الطلب في لوحة الأدمن:

يتحدث وضع الطلب في واجهة العميل (الخط الزمني لاستيراد الشحنة).

ربط CRM / العملاء

تأكد أن كل عميل CustomerAccount مرتبط بكل طلباته:

عروض أسعار

نواقص

استيراد

في صفحة AdminCustomersPage يمكن رؤية ملخص لنشاط كل عميل.

ثالثًا: تفعيل البحث والذكاء الاصطناعي و Normalization

تأكد أن كل الصفحات التي فيها بحث عن رقم قطعة أو اسم:

تستخدم دالة normalizePartNumber (من partNumberUtils.ts) قبل المقارنة،
بحيث:

إزالة الفراغات

توحيد الأحرف

تجاهل الشرطات والرموز غير المهمة

التأكد من استخدام أدوات البحث العربي في arabicSearch.ts في الشاشات التي تعرض أسماء عربية.

لو فيه زر أو خيار لـ "بحث ذكي" أو "Smart Search" تأكد أنه يعمل، أو اترك هيكله موجودًا لكن بدون تعطيل أي شيء آخر.

رابعًا: الإشعارات + الـ Timeline

تأكد أن مكون NotificationBell.tsx:

يقرأ من ActivityLogEntry أو مصدر النشاط.

يعرض عدد الإشعارات الجديدة.

يفتح قائمة بآخر الأحداث.

تأكد أن أي عملية مهمة (إنشاء عرض سعر، طلب نقص، طلب استيراد، تغيير حالة، تعليق حساب، تفعيل حساب…) تسجل في السجل مع:

التاريخ

نوع الحدث

المستخدم الذي قام به (عميل / موظف / أدمن)

استخدم dateUtils لتنسيق التاريخ والوقت.

خامسًا: استيراد المنتجات من Excel

يوجد منطق أو صفحة لاستيراد المنتجات من ملف Excel (صيغة Onyx Pro).

تأكد أن:

الأعمدة المطلوبة (رقم الصنف، اسم الصنف، مستويات التسعير، الكمية...) تقرأ بشكل صحيح.

يتم تحويلها إلى Product وفق النوع الموجود في types.ts.

يتم إضافتها إلى البيانات الحالية بدون حذف منتجات موجودة مسبقًا.

سادسًا: الصلاحيات والأدوار (Permissions)

تأكد أن:

admin يمكنه رؤية كل شيء.

employee يسمح له فقط بما تحدده الأنواع أو المنطق الحالي (مثلاً إدارة الطلبات فقط، أو التسعير فقط).

customer لا يرى صفحات الأدمن ولا أي بيانات تخص عملاء آخرين.

استخدم الحقول الموجودة في UserRole / EmployeeRole / CustomerAccount بدون تغيير أسمائها.

🧪 5) فحص شامل للموقع (QA)

أريدك بعد إكمال الربط والإصلاح أن تقوم بالتالي:

افتح كل صفحة في المشروع (عميل + أدمن).

جرّب كل زر:

إنشاء عرض سعر

إضافة عنصر للصنف في عرض السعر

إرسال الطلب

تغيير حالة الطلب

إنشاء طلب نقص

إنشاء طلب استيراد

إضافة عميل

تعديل بيانات عميل

تغيير صلاحيات موظف

استيراد ملف Excel (لو متوفر)

تأكد أنه لا يوجد:

أخطاء في الكونسول (Console errors)

تحذيرات TypeScript خطيرة (Type errors)

كراش أو صفحات بيضاء بلا سبب

أي خطأ تجده:

أصلحه من دون حذف المنطق أو تبسيطه بشكل يلغيه.

إذا احتجت تضيف حقل جديد أو حالة جديدة، أضفها بأسلوب متوافق مع الموجود.

📌 ملخص نهائي

لا تعيد بناء المشروع.

لا تحذف شيء.

لا تغيّر Types أو مفاتيح LocalStorage.

اربط لوحة العميل بلوحة الأدمن في:

عروض الأسعار

النواقص

الاستيراد من الصين

CRM

Activity Log + Notifications

فعّل البحث والـ Normalization لأرقام القطع في كل الصفحات المتعلقة.

كمّل النواقص الصغيرة وتأكد أن كل الأزرار تعمل بشكل صحيح.

الهدف النهائي:
نظام SINI CAR B2B جاهز للاستخدام الفعلي كتجربة أولية، متكامل بين العميل والأدمن، ومبني على الكود الحالي فقط.