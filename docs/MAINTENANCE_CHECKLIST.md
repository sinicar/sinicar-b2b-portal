# قائمة التحقق للصيانة - SINI CAR B2B

## 🚨 قبل أي Pull Request

```bash
npm run verify   # يشغل: check:deps + typecheck + build
```

**يجب أن ينجح هذا الأمر قبل فتح أي PR**

---

## 🔧 خطوات التحقق قبل أي تغيير

### 1. Frontend (Vite + React)

```bash
cd "d:\SINI CAR B2B"
npm run verify       # الأمر الشامل (الأهم)
npm run typecheck    # تحقق من الأنواع فقط
npm run build        # بناء الإنتاج فقط
npm run check:deps   # فحص circular imports
npm run dev          # تشغيل التطوير على localhost:3000
```

### 2. Backend (Express + Prisma)

```bash
cd "d:\SINI CAR B2B\backend"
npm run build        # بناء TypeScript
npm run dev          # تشغيل على localhost:3005
```

### 3. قاعدة البيانات (Prisma)

```bash
cd "d:\SINI CAR B2B\backend"
npx prisma generate   # توليد Client
npx prisma db push    # مزامنة Schema (للتطوير فقط)
npx prisma migrate deploy  # تطبيق Migrations (للإنتاج)
```

---

## ✅ اختبار سريع بعد التغييرات (Smoke Test)

### الخطوة 1: تشغيل الخوادم

```bash
# Terminal 1 - Backend
cd "d:\SINI CAR B2B\backend" && npm run dev

# Terminal 2 - Frontend
cd "d:\SINI CAR B2B" && npm run dev
```

### الخطوة 2: اختبار البوابات

| البوابة  | الإجراء                   | النتيجة المتوقعة      |
| -------- | ------------------------- | --------------------- |
| Login    | افتح localhost:3000       | صفحة تسجيل الدخول     |
| Admin    | سجل دخول كـ أدمن (user-1) | لوحة القيادة المركزية |
| Customer | سجل دخول كـ عميل (user-2) | واجهة العميل          |
| Supplier | سجل دخول كـ مورد (user-5) | بوابة المورد          |

### الخطوة 3: التحقق من Console

- لا أخطاء JavaScript في المتصفح
- لا أخطاء 500 في الشبكة

---

## 🚫 قواعد صارمة

1. **لا تغيير في API Contracts** - نفس الـ endpoints و response shapes
2. **لا MockApi** - تم إزالته بالكامل ✅
3. **لا Guest Mode** - تم إزالته بالكامل ✅
4. **اختبار بعد كل تغيير** - لا تجمع تغييرات متعددة

---

## 🗑 الكود المحذوف (30 ديسمبر 2025)

| الكود                     | الحالة   | الملاحظات                |
| ------------------------- | -------- | ------------------------ |
| `MockApi`                 | ✅ محذوف | 0 استخدام في src/        |
| `isGuest`                 | ✅ محذوف | 0 استخدام في src/        |
| `handleGuestLogin`        | ✅ محذوف | من App.tsx               |
| `showGuestPrompt`         | ✅ محذوف | من Dashboard.tsx         |
| `Guest Restriction Modal` | ✅ محذوف | 112 سطر من Dashboard.tsx |
| `isSectionVisible`        | ✅ محذوف | كان للـ Guest فقط        |

---

## 📁 الهيكل الأساسي

```
SINI CAR B2B/
├── src/                    # Frontend React
│   ├── components/         # مكونات UI
│   ├── services/           # API و الخدمات
│   └── types/              # تعريفات TypeScript
├── backend/                # Backend Express
│   ├── src/modules/        # الوحدات (users, products, etc.)
│   └── prisma/             # قاعدة البيانات
└── docs/                   # التوثيق
```

---

## 🔄 عملية التحقق الكاملة

```bash
# 1. تحقق من الأنواع
npm run typecheck

# 2. بناء Frontend
npm run build

# 3. بناء Backend
cd backend && npm run build && cd ..

# 4. تشغيل واختبار
# (شغل الخوادم واختبر يدوياً)
```

---

## 🛡️ Permissions Center Regression Checklist

عند تعديل أي شيء في مركز الصلاحيات، تحقق من:

### Staff Tab (موظفي المنصة)

- [ ] عرض قائمة الموظفين
- [ ] إضافة موظف جديد
- [ ] تبديل حالة الموظف (فعال/معطل)
- [ ] حذف موظف
- [ ] عرض قائمة الأدوار
- [ ] إضافة دور جديد
- [ ] تعديل دور (تغيير الصلاحيات)
- [ ] حذف دور (غير نظامي)

### Customers Tab (صلاحيات العملاء)

- [ ] عرض قائمة الميزات
- [ ] تبديل الميزات (toggle)
- [ ] حفظ الإعدادات الافتراضية

### Suppliers Tab (صلاحيات الموردين)

- [ ] عرض قائمة الميزات
- [ ] تبديل الميزات (toggle)
- [ ] حفظ الإعدادات الافتراضية

### Overrides Tab (استثناءات فردية)

- [ ] عرض قائمة الاستثناءات
- [ ] إضافة استثناء جديد
- [ ] تعديل استثناء
- [ ] حذف استثناء

### Organizations Tab (إعدادات المنظمات)

- [ ] عرض قائمة المنظمات
- [ ] تعديل إعدادات المنظمة

### General

- [ ] تبديل بين التبويبات يعمل
- [ ] البحث يعمل (فلترة النتائج)
- [ ] مسح البحث يعيد كل النتائج
- [ ] الـ Toast messages تظهر
- [ ] لا أخطاء في Console

### Toggle Operations

- [ ] Single toggle (تبديل صلاحية واحدة)
- [ ] Bulk/Section toggle (تبديل مجموعة)
- [ ] Toggle state يُحفظ بعد Refresh

### Save Operations

- [ ] Save button مُفعّل عند وجود تغييرات
- [ ] Save button مُعطّل عند عدم وجود تغييرات
- [ ] رسالة نجاح بعد الحفظ
- [ ] رسالة خطأ عند فشل الحفظ (إن وُجد)

### Role/Tab Switching

- [ ] تبديل الأدوار يحافظ على الحالة
- [ ] تبديل التبويبات لا يضيع البيانات غير المحفوظة
- [ ] Sub-tabs (Users/Roles) تعمل بشكل صحيح

---

## 🤖 Admin AI Training Regression Checklist

عند تعديل صفحة تدريب الذكاء الاصطناعي، تحقق من:

### Page Load

- [ ] الصفحة تفتح بدون أخطاء
- [ ] جميع العدادات (Knowledge, Conversations, Prompts, Tests) تظهر
- [ ] لا أخطاء في Console

### Tab Navigation

- [ ] تبديل التبويبات يعمل (Knowledge/Conversations/Prompts/Testing/Analytics)
- [ ] الـ Tab النشط يُميّز بشكل صحيح
- [ ] المحتوى يتغير حسب الـ Tab

### Knowledge Base Tab

- [ ] عرض قائمة المعارف
- [ ] البحث يفلتر النتائج
- [ ] تصفية حسب الفئة
- [ ] إضافة معرفة جديدة
- [ ] تعديل معرفة
- [ ] حذف معرفة

### Conversations Tab

- [ ] عرض قائمة المحادثات
- [ ] إضافة محادثة جديدة
- [ ] عرض تفاصيل المحادثة
- [ ] حذف محادثة

### Prompts Tab

- [ ] عرض قائمة البرومبتات
- [ ] إضافة برومبت جديد
- [ ] تفعيل/تعطيل برومبت
- [ ] تعديل برومبت
- [ ] حذف برومبت

### Testing Tab

- [ ] عرض قائمة حالات الاختبار
- [ ] تشغيل اختبار فردي
- [ ] تشغيل جميع الاختبارات
- [ ] عرض نتائج الاختبار

### Export/Import

- [ ] زر التصدير يفتح نافذة التحميل
- [ ] زر الاستيراد يقبل ملف JSON
- [ ] البيانات تُستورد بشكل صحيح
- [ ] رسالة نجاح/خطأ تظهر

---

## 📦 Supplier Portal Regression Checklist

عند تعديل بوابة الموردين، تحقق من:

### Page Load

- [ ] البوابة تفتح بدون أخطاء
- [ ] الـ Dashboard stats تظهر (منتجات، طلبات، إيرادات)
- [ ] لا أخطاء في Console

### Navigation

- [ ] الـ Sidebar تعمل (expand/collapse)
- [ ] تبديل الـ views يعمل:
  - [ ] Dashboard
  - [ ] Products
  - [ ] Purchase Orders
  - [ ] Notifications
  - [ ] Team
  - [ ] Settings
- [ ] زر mobile sidebar يعمل

### Header

- [ ] العنوان يتغير حسب الـ view
- [ ] الوقت يظهر (desktop)
- [ ] NotificationBell يعمل
- [ ] LanguageSwitcher يعمل
- [ ] تسجيل الخروج يعمل

### Products Tab

- [ ] عرض قائمة المنتجات
- [ ] إضافة منتج جديد
- [ ] تعديل منتج
- [ ] تصدير المنتجات (Excel)
- [ ] استيراد المنتجات (إن وُجد)
- [ ] Pagination يعمل

### Purchase Orders Tab

- [ ] عرض قائمة أوامر الشراء
- [ ] توسيع/طي تفاصيل الطلب
- [ ] تغيير حالة الطلب (NEW → PREPARING → READY → SHIPPED)
- [ ] عرض العناصر داخل الطلب
- [ ] Empty state يظهر عند عدم وجود طلبات

### Settings Tab

- [ ] عرض إعدادات المورد
- [ ] تعديل الإعدادات
- [ ] حفظ الإعدادات

### General

- [ ] RTL/LTR يعمل بشكل صحيح
- [ ] Responsive design (mobile/desktop)
- [ ] Toast messages تظهر للإجراءات
- [ ] Loading states تظهر

---

## 🛒 Customer Dashboard Regression Checklist

عند تعديل لوحة تحكم العميل، تحقق من:

### Page Load

- [ ] الصفحة تفتح بدون أخطاء
- [ ] الـ KPIs تظهر (طلبات، عروض أسعار، سلة)
- [ ] لا أخطاء في Console

### Navigation

- [ ] الـ Sidebar تعمل (expand/collapse على desktop)
- [ ] تبديل الـ views يعمل:
  - [ ] HOME (الصفحة الرئيسية)
  - [ ] ORDERS (الطلبات)
  - [ ] QUOTE_REQUEST (طلب عرض سعر)
  - [ ] ORGANIZATION (المنظمة)
  - [ ] HISTORY (السجل)
  - [ ] NOTIFICATIONS (الإشعارات)
- [ ] Mobile sidebar overlay يعمل

### Header

- [ ] شعار الشركة يظهر
- [ ] NotificationBell يعمل
- [ ] Cart icon يظهر عدد العناصر
- [ ] LanguageSwitcher يعمل
- [ ] تسجيل الخروج يعمل

### Search

- [ ] Hero search bar يعمل
- [ ] نتائج البحث تظهر
- [ ] عرض السعر يستهلك credit
- [ ] إضافة للسلة من نتائج البحث

### Cart

- [ ] إضافة منتج للسلة
- [ ] حذف منتج من السلة
- [ ] تأكيد العنصر المكرر
- [ ] إرسال الطلب يعمل
- [ ] Success modal يظهر

### Quick Actions

- [ ] أزرار الإجراءات السريعة تعمل
- [ ] تنقل صحيح للصفحات المستهدفة

### General

- [ ] RTL/LTR يعمل بشكل صحيح
- [ ] Responsive design (mobile/desktop)
- [ ] Toast messages تظهر للإجراءات
- [ ] Flying cart animation تعمل
