# هندسة النظام - SINI CAR B2B Platform

## 📊 نظرة عامة

```
┌─────────────────────────────────────────────────────────────┐
│                      Frontend (Vite + React)                │
│                      localhost:3000                         │
├─────────────────────────────────────────────────────────────┤
│  App.tsx → AdminDashboard / Dashboard / SupplierPortal     │
│            ↓                                                │
│  services/api.ts ←→ services/httpClient.ts                  │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTP (fetch)
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    Backend (Express + Prisma)               │
│                      localhost:3005                         │
├─────────────────────────────────────────────────────────────┤
│  src/index.ts → Routes → Controllers → Services → Repos    │
│                                          ↓                  │
│                                    Prisma Client            │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    PostgreSQL Database                      │
│                    (Neon / Supabase)                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 هيكل Frontend

```
src/
├── components/           # مكونات React
│   ├── Dashboard.tsx     # بوابة العملاء
│   ├── AdminDashboard.tsx # لوحة الإدارة
│   ├── SupplierPortal.tsx # بوابة الموردين
│   └── dashboard/        # مكونات فرعية
├── services/
│   ├── api.ts            # واجهة API الرئيسية (4000+ سطر)
│   ├── httpClient.ts     # HTTP client wrapper
│   └── serviceFactory.ts # Factory للخدمات
├── types/
│   └── *.ts              # تعريفات TypeScript
└── App.tsx               # Entry point + Routing
```

---

## 📁 هيكل Backend

```
backend/
├── src/
│   ├── index.ts          # Express entry point
│   ├── modules/          # الوحدات الوظيفية
│   │   ├── users/        # إدارة المستخدمين
│   │   ├── products/     # إدارة المنتجات
│   │   ├── orders/       # إدارة الطلبات
│   │   ├── suppliers/    # إدارة الموردين
│   │   └── ...           # وحدات أخرى
│   └── middleware/       # Express middleware
├── prisma/
│   ├── schema.prisma     # تعريف الجداول
│   └── migrations/       # سجل التغييرات
└── package.json
```

---

## 🔄 تدفق البيانات

```
User Action → React Component → services/api.ts → httpClient.ts
                                      ↓
                              Backend Route Handler
                                      ↓
                              Service Layer
                                      ↓
                              Repository (Prisma)
                                      ↓
                              PostgreSQL
```

---

## 🔐 المصادقة

| المكون         | الوظيفة                      |
| -------------- | ---------------------------- |
| `App.tsx`      | Session restore + Login flow |
| `api.login()`  | POST /api/auth/login         |
| `localStorage` | تخزين session token          |
| Backend JWT    | التحقق من الهوية             |

---

## 🚫 قواعد صارمة للـ Refactor

### ✅ مسموح:

- استخراج functions إلى ملفات منفصلة
- إعادة تنظيم المجلدات
- إضافة توثيق
- إضافة re-exports للحفاظ على التوافق

### ❌ ممنوع:

- تغيير API endpoints
- تغيير response shapes
- إزالة functions موجودة (استخدم re-export)
- تغيير سلوك UI

---

## 📝 أمثلة Re-export للتوافق

```typescript
// services/api/modules/orders.ts (جديد)
export const getOrders = async (userId: string) => { ... };

// services/api.ts (الأصلي)
export { getOrders } from './api/modules/orders';
// أو
import { getOrders as getOrdersNew } from './api/modules/orders';
export const getOrders = getOrdersNew; // نفس الاسم
```

---

## 🧪 اختبار بعد كل تغيير

```bash
npm run build          # ✅ يجب أن ينجح
npm run dev            # ✅ يجب أن يعمل
# اختبر البوابات الثلاث يدوياً
```
