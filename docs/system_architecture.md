# 🏗️ هيكل نظام صيني كار الكامل

## 📌 نظرة عامة

نظام **صيني كار** هو منصة B2B لتجارة قطع غيار السيارات الصينية بالجملة. يربط بين:
- **العملاء** (محلات، ورش، شركات تأجير، شركات تأمين)
- **الموردين الداخليين** (مسجلين في النظام)
- **الموردين الخارجيين** (غير مسجلين - تواصل يدوي)
- **إدارة صيني كار** (فريق العمل الداخلي)

---

## 🔄 المخطط الرئيسي للنظام

```mermaid
flowchart TB
    subgraph CUSTOMERS["👥 العملاء"]
        C1[محل قطع غيار]
        C2[ورشة صيانة]
        C3[شركة تأمين]
        C4[شركة تأجير]
    end

    subgraph SINICAR["🏢 صيني كار"]
        SC1[بوابة العميل]
        SC2[لوحة الإدارة]
        SC3[المستودع]
    end

    subgraph SUPPLIERS["📦 الموردين"]
        S1["مورد داخلي ✅<br/>(مسجل في النظام)"]
        S2["مورد خارجي ⚠️<br/>(غير مسجل)"]
    end

    CUSTOMERS --> SC1
    SC1 <--> SC2
    SC2 <--> SC3
    SC2 <--> S1
    SC2 -.-> |تواصل يدوي| S2
```

---

## 👤 رحلة العميل الكاملة

```mermaid
flowchart TD
    START([العميل يدخل المنصة]) --> LOGIN{مسجل؟}
    
    LOGIN -->|نعم| DASHBOARD[لوحة تحكم العميل]
    LOGIN -->|لا| REGISTER[طلب فتح حساب]
    REGISTER --> ADMIN_REVIEW[مراجعة الإدارة]
    ADMIN_REVIEW --> APPROVED{موافقة؟}
    APPROVED -->|نعم| DASHBOARD
    APPROVED -->|لا| REJECTED[مرفوض + سبب]
    
    DASHBOARD --> SEARCH[البحث عن قطعة]
    SEARCH --> FOUND{نتائج؟}
    
    FOUND -->|نعم| CHECK_STOCK{نوع التوفر}
    FOUND -->|لا| MISSING_REQUEST[طلب قطعة ناقصة ❌]
    
    CHECK_STOCK -->|IN_STOCK| ADD_CART[إضافة للسلة]
    CHECK_STOCK -->|ORDER| ORDER_ITEM["إضافة للسلة<br/>(طلبية 48 ساعة)"]
    
    ADD_CART --> CART[سلة المشتريات]
    ORDER_ITEM --> CART
    
    CART --> CHECKOUT[إتمام الطلب]
    CHECKOUT --> ORDER_CREATED[تم إنشاء الطلب]
    
    ORDER_CREATED --> HAS_ORDER_ITEMS{يحتوي منتجات ORDER؟}
    HAS_ORDER_ITEMS -->|نعم| CREATE_SHORTAGE[إنشاء سجل نقص طلبية 📦]
    HAS_ORDER_ITEMS -->|لا| DIRECT_FULFILL[تنفيذ مباشر من المخزن]
    
    CREATE_SHORTAGE --> DELIVERY[التوصيل للعميل]
    DIRECT_FULFILL --> DELIVERY
```

---

## 📦 أنواع الموردين

### المورد الداخلي (مسجل في النظام) ✅

| الخاصية | القيمة |
|---------|--------|
| `storageSection` | `'SUPPLIER'` |
| `supplierId` | معرف المورد في النظام |
| `availabilityType` | `'IN_STOCK'` عادةً |
| التواصل | تلقائي عبر النظام |
| أوامر الشراء | تظهر في بوابة المورد |

### المورد الخارجي (غير مسجل) ⚠️

| الخاصية | القيمة |
|---------|--------|
| `storageSection` | `'ORDER_PRODUCTS'` |
| `unregisteredSupplierName` | اسم المورد |
| `unregisteredSupplierContact` | رقم الجوال |
| `availabilityType` | `'ORDER'` |
| `deliveryHours` | وقت التوصيل (24، 48، 72...) |

---

## 🔀 تدفق الطلبات حسب مصدر المنتج

```mermaid
flowchart TD
    ORDER[طلب جديد] --> ITEMS[تحليل المنتجات]
    
    ITEMS --> |مخزن صيني كار| WAREHOUSE["🏭 SINICAR_WAREHOUSE"]
    ITEMS --> |مورد مسجل| SUPPLIER["✅ SUPPLIER"]
    ITEMS --> |مورد خارجي| ORDER_PRODUCTS["⚠️ ORDER_PRODUCTS"]
    
    WAREHOUSE --> W1[تجهيز من المستودع]
    W1 --> W2[شحن مباشر]
    
    SUPPLIER --> S1[إرسال أمر شراء للمورد]
    S1 --> S2[المورد يجهز]
    S2 --> S3[استلام في صيني كار]
    S3 --> S4[شحن للعميل]
    
    ORDER_PRODUCTS --> O1[إنشاء سجل OrderShortage]
    O1 --> O2[موظف يتواصل مع المورد]
    O2 --> O3[تأكيد التوفر والسعر]
    O3 --> O4[المورد يجهز]
    O4 --> O5[استلام في صيني كار]
    O5 --> O6[شحن للعميل]
```

---

## 👨‍💼 صفحات الإدارة

| الصفحة | الغرض |
|--------|-------|
| **طلبات العملاء** | مراجعة وتنفيذ الطلبات |
| **طلبات الحسابات** | الموافقة على حسابات جديدة |
| **طلبات التسعير** | تسعير قوائم Excel |
| **النواقص** ❌ | البحث عن قطع غير موجودة |
| **نواقص الطلبيات** 📦 | متابعة موردين خارجيين |
| **المنتجات** | إضافة وتعديل المنتجات |
| **سوق الموردين** | إدارة الموردين المسجلين |

---

## 🔔 حالات نقص الطلبية (OrderShortage)

```mermaid
stateDiagram-v2
    [*] --> NEW: طلبية جديدة
    NEW --> CONTACTING: جاري التواصل
    CONTACTING --> CONFIRMED: المورد أكد
    CONTACTING --> CANCELLED: غير متوفر
    CONFIRMED --> PREPARING: قيد التحضير
    PREPARING --> RECEIVED: صيني كار استلمت
    RECEIVED --> DELIVERED: تم التسليم للعميل
    DELIVERED --> [*]
    CANCELLED --> [*]
```

---

## 💰 معادلة التسعير

```
سعر البيع = سعر الشراء × (1 + هامش الربح / 100)
```

---

## ✅ ملخص الفروقات

| المفهوم | الوصف | الصفحة |
|---------|-------|--------|
| **مخزون صيني كار** | قطع في مستودع صيني كار | تُشحن مباشرة |
| **مورد داخلي** | مورد مسجل في النظام | بوابة الموردين |
| **مورد خارجي** | مورد غير مسجل | نواقص الطلبيات |
| **قطعة ناقصة** | قطعة غير موجودة في الكتالوج | النواقص |
| **طلبية** | قطعة موجودة لكن من مورد خارجي | نواقص الطلبيات |
