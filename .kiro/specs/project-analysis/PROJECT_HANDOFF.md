# 📋 وثيقة تسليم مشروع صيني كار B2B Portal

## للمبرمج الجديد - كل ما تحتاج معرفته

---

## 🎯 ما هو هذا المشروع؟

**صيني كار** هو نظام تجارة إلكترونية B2B (من شركة لشركة) متخصص في بيع قطع غيار السيارات الصينية بالجملة.

### الفكرة الأساسية
تخيل أنك تملك شركة تبيع قطع غيار سيارات صينية (شانجان، MG، هافال، إلخ). لديك:
- **عملاء** = محلات قطع غيار، ورش صيانة، شركات تأمين، شركات تأجير
- **موردين** = موردين محليين ودوليين يوفرون القطع
- **فريق إدارة** = موظفين يديرون الطلبات والعملاء

هذا النظام يربط الجميع في منصة واحدة.

---

## 👥 من يستخدم النظام؟

### 1. العملاء (Customers)
| النوع | الوصف |
|-------|-------|
| محل قطع غيار | يشتري بالجملة لإعادة البيع |
| ورشة صيانة | يشتري للاستخدام في الصيانة |
| شركة تأمين | تشتري قطع لإصلاح سيارات التأمين |
| شركة تأجير | تشتري لصيانة أسطولها |
| مندوب مبيعات | يبيع لعملاء آخرين |

### 2. الموردين (Suppliers)
| النوع | الوصف |
|-------|-------|
| مورد محلي | مسجل في النظام، يستلم طلبات تلقائياً |
| مورد دولي | للاستيراد من الصين |

### 3. الإدارة (Admin)
| الدور | المسؤوليات |
|-------|-----------|
| مشرف عام | صلاحيات كاملة |
| موظف | صلاحيات محدودة حسب الدور |

---

## 🔄 كيف يعمل النظام؟

### رحلة العميل الأساسية
```
1. العميل يسجل حساب جديد
   ↓
2. الإدارة تراجع وتوافق
   ↓
3. العميل يدخل ويبحث عن قطع
   ↓
4. يضيف للسلة ويرسل الطلب
   ↓
5. الإدارة تراجع وتوافق
   ↓
6. يتم تجهيز وشحن الطلب
   ↓
7. العميل يستلم
```

---

## 📱 الصفحات والشاشات الرئيسية

### بوابة العميل (Customer Portal)

| الصفحة | الوظيفة | الملف |
|--------|---------|-------|
| **الرئيسية** | عرض المنتجات والعروض | `Dashboard.tsx` |
| **البحث** | البحث عن قطع برقم القطعة أو الاسم | `ProductSearchPage.tsx` |
| **السلة** | عرض المنتجات المختارة | داخل `Dashboard.tsx` |
| **طلباتي** | عرض الطلبات السابقة وحالتها | `OrdersPage.tsx` |
| **طلب تسعير** | رفع ملف Excel للحصول على أسعار | `QuoteRequestPage.tsx` |
| **الاستيراد** | طلب استيراد من الصين | `ImportFromChinaPage.tsx` |
| **فريقي** | إدارة الموظفين والفروع | `TeamManagementPage.tsx` |
| **التقسيط** | طلب شراء بالتقسيط | `CustomerInstallmentPage.tsx` |
| **الأدوات** | أدوات مساعدة للتاجر | `TraderToolsHub.tsx` |

### لوحة الإدارة (Admin Dashboard)

| الصفحة | الوظيفة | الملف |
|--------|---------|-------|
| **الرئيسية** | إحصائيات ومؤشرات | `AdminDashboard.tsx` |
| **العملاء** | إدارة العملاء | `AdminCustomersPage.tsx` |
| **الطلبات** | إدارة الطلبات | `AdminOrdersManager.tsx` |
| **المنتجات** | إدارة المنتجات | `AdminProductsPage.tsx` |
| **التسعير** | طلبات التسعير | `AdminQuoteManager.tsx` |
| **الاستيراد** | طلبات الاستيراد | `AdminImportManager.tsx` |
| **النواقص** | القطع غير المتوفرة | `AdminMissingParts.tsx` |
| **التسويق** | الحملات والإعلانات | `AdminMarketingCenter.tsx` |
| **الإعدادات** | إعدادات النظام | `AdminSettings.tsx` |

### بوابة المورد (Supplier Portal)

| الصفحة | الوظيفة | الملف |
|--------|---------|-------|
| **الرئيسية** | لوحة تحكم المورد | `SupplierPortal.tsx` |
| **المنتجات** | كتالوج المورد | داخل `SupplierPortal.tsx` |
| **الطلبات** | طلبات الشراء الواردة | داخل `SupplierPortal.tsx` |

---

## 💡 الميزات الخاصة

### 1. نظام نقاط البحث (Search Points)
- كل عميل لديه رصيد نقاط
- كشف السعر يستهلك نقطة
- يمكن للإدارة منح نقاط إضافية

### 2. مستويات التسعير (Price Levels)
```
LEVEL_1 = سعر التجزئة (أعلى سعر)
LEVEL_2 = سعر الجملة
LEVEL_3 = سعر جملة الجملة (أقل سعر)
SPECIAL = سعر خاص (مخصص)
```

### 3. أدوات التاجر (Trader Tools)
| الأداة | الوظيفة |
|--------|---------|
| VIN Extractor | استخراج بيانات السيارة من رقم الشاسيه |
| مقارنة الأسعار | مقارنة أسعار القطع |
| PDF to Excel | تحويل ملفات PDF لـ Excel |

### 4. نظام التقسيط
- العميل يطلب تقسيط
- صيني كار توافق أو ترفض
- يمكن تحويل الطلب للموردين
- الموردين يقدمون عروض
- العميل يختار العرض المناسب

### 5. نظام الإشعارات
- إشعارات فورية داخل التطبيق
- تتبع حالة الطلبات
- تنبيهات للإدارة عند وجود طلبات جديدة


---

## 🛠️ التقنيات المستخدمة

### Frontend (الواجهة الأمامية)
| التقنية | الإصدار | الغرض |
|---------|---------|-------|
| React | 19.2 | إطار العمل الرئيسي |
| TypeScript | 5.8 | لغة البرمجة |
| Vite | 6.4 | أداة البناء والتطوير |
| Tailwind CSS | 4.1 | التنسيق والتصميم |
| Zustand | 5.0 | إدارة الحالة |
| i18next | 25.7 | الترجمة متعددة اللغات |
| Wouter | 3.8 | التوجيه (Routing) |
| Framer Motion | 12.23 | الحركات والانتقالات |
| Recharts | 2.15 | الرسوم البيانية |

### Backend (الواجهة الخلفية)
| التقنية | الإصدار | الغرض |
|---------|---------|-------|
| Express.js | 4.21 | إطار الخادم |
| Prisma | 5.22 | ORM لقاعدة البيانات |
| PostgreSQL | - | قاعدة البيانات |
| JWT | 9.0 | المصادقة |
| Zod | 3.23 | التحقق من البيانات |
| bcryptjs | 2.4 | تشفير كلمات المرور |

---

## 🚀 كيفية تشغيل المشروع

### المتطلبات الأساسية
- Node.js v18 أو أحدث
- npm أو pnpm
- PostgreSQL (للـ Backend)

### خطوات التشغيل

#### 1. تشغيل الـ Frontend
```bash
# تثبيت المكتبات
npm install

# تشغيل في وضع التطوير
npm run dev

# التطبيق سيعمل على http://localhost:5173
```

#### 2. تشغيل الـ Backend
```bash
# الانتقال لمجلد الـ backend
cd backend

# تثبيت المكتبات
npm install

# إعداد قاعدة البيانات
npx prisma generate
npx prisma db push

# تشغيل الخادم
npm run dev

# الخادم سيعمل على http://localhost:3000
```

### ملفات البيئة (.env)

#### Frontend (.env)
```env
VITE_API_URL=http://localhost:3000/api/v1
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_key
```

#### Backend (backend/.env)
```env
DATABASE_URL="postgresql://user:password@localhost:5432/sinicar"
JWT_SECRET="your_jwt_secret"
PORT=3000
```

---

## 🗄️ هيكل قاعدة البيانات

### الجداول الرئيسية

```
┌─────────────────┐     ┌─────────────────┐
│      User       │────▶│ BusinessProfile │
├─────────────────┤     ├─────────────────┤
│ id              │     │ id              │
│ clientId        │     │ userId          │
│ name            │     │ companyName     │
│ email           │     │ crNumber        │
│ phone           │     │ taxNumber       │
│ role            │     │ priceLevel      │
│ status          │     │ searchLimit     │
└─────────────────┘     └─────────────────┘
         │
         ▼
┌─────────────────┐     ┌─────────────────┐
│      Order      │────▶│    OrderItem    │
├─────────────────┤     ├─────────────────┤
│ id              │     │ id              │
│ orderNumber     │     │ orderId         │
│ userId          │     │ productId       │
│ status          │     │ quantity        │
│ totalAmount     │     │ price           │
└─────────────────┘     └─────────────────┘
```

### العلاقات الرئيسية
- `User` → `BusinessProfile` (1:1)
- `User` → `Order` (1:N)
- `Order` → `OrderItem` (1:N)
- `User` → `Organization` (N:M)
- `Role` → `Permission` (N:M)

---

## 🔌 نقاط الـ API الرئيسية

### المصادقة (Auth)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| POST | `/api/v1/auth/login` | تسجيل الدخول |
| POST | `/api/v1/auth/register` | تسجيل حساب جديد |
| POST | `/api/v1/auth/logout` | تسجيل الخروج |
| GET | `/api/v1/auth/me` | بيانات المستخدم الحالي |

### الطلبات (Orders)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/v1/orders` | قائمة الطلبات |
| POST | `/api/v1/orders` | إنشاء طلب جديد |
| GET | `/api/v1/orders/:id` | تفاصيل طلب |
| PATCH | `/api/v1/orders/:id/status` | تحديث حالة الطلب |

### المنتجات (Products)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/v1/products` | قائمة المنتجات |
| GET | `/api/v1/products/search` | البحث عن منتجات |
| GET | `/api/v1/products/:id` | تفاصيل منتج |

### العملاء (Customers)
| Method | Endpoint | الوصف |
|--------|----------|-------|
| GET | `/api/v1/customers` | قائمة العملاء |
| GET | `/api/v1/customers/:id` | تفاصيل عميل |
| PATCH | `/api/v1/customers/:id/approve` | الموافقة على عميل |

---

## 🔐 نظام المصادقة

### تدفق تسجيل الدخول
```
1. المستخدم يدخل (رقم العميل/الهاتف) + كلمة المرور
   ↓
2. الخادم يتحقق من البيانات
   ↓
3. إذا صحيحة: يُنشئ JWT token
   ↓
4. يُرسل Token للـ Frontend
   ↓
5. Frontend يحفظ Token في localStorage
   ↓
6. كل طلب API يُرسل Token في Header
```

### أنواع تسجيل الدخول
- **OWNER**: صاحب الحساب (يدخل برقم العميل)
- **STAFF**: موظف (يدخل برقم الهاتف)

### التحقق من الصلاحيات
```typescript
// مثال على التحقق من الصلاحية
const hasPermission = (user, permission) => {
  return user.permissions.includes(permission);
};

// الصلاحيات الرئيسية
'orders.view'      // عرض الطلبات
'orders.create'    // إنشاء طلبات
'orders.approve'   // الموافقة على الطلبات
'customers.manage' // إدارة العملاء
'products.manage'  // إدارة المنتجات
```

---

## 📁 هيكل الملفات المهمة

```
📁 src/
├── 📄 App.tsx                 # نقطة الدخول الرئيسية
├── 📄 types.ts                # جميع الأنواع (4391 سطر - يحتاج تقسيم)
│
├── 📁 components/             # المكونات (70+ ملف)
│   ├── 📄 Dashboard.tsx       # لوحة تحكم العميل
│   ├── 📄 AdminDashboard.tsx  # لوحة تحكم الإدارة
│   └── ...
│
├── 📁 services/               # الخدمات
│   ├── 📄 mockApi.ts          # API وهمي (6000+ سطر - يحتاج تقسيم)
│   ├── 📄 realApi.ts          # API حقيقي
│   ├── 📄 pricingEngine.ts    # محرك التسعير
│   └── ...
│
├── 📁 stores/                 # Zustand stores
│   └── 📄 useAuthStore.ts     # حالة المصادقة
│
└── 📁 locales/                # ملفات الترجمة
    ├── 📄 ar.json             # العربية
    ├── 📄 en.json             # الإنجليزية
    ├── 📄 hi.json             # الهندية
    └── 📄 zh.json             # الصينية
```

---

## 🔑 بيانات الدخول للتجربة

### حساب المدير
```
اسم المستخدم: admin
كلمة المرور: admin
```

### حساب عميل
```
رقم العميل: 1
كلمة المرور: 1
```

---

## 💼 المنطق التجاري المهم

### 1. حساب السعر
```typescript
// السعر يعتمد على مستوى العميل
function calculatePrice(product, customer) {
  switch (customer.priceLevel) {
    case 'LEVEL_1': return product.priceRetail;
    case 'LEVEL_2': return product.priceWholesale;
    case 'LEVEL_3': return product.priceWholesale * 0.9;
    case 'SPECIAL': return customer.specialPrice || product.priceWholesale;
  }
}
```

### 2. نقاط البحث
```typescript
// كل كشف سعر يستهلك نقطة
function revealPrice(customer, product) {
  if (customer.searchUsed >= customer.searchLimit) {
    throw new Error('لا يوجد رصيد كافٍ');
  }
  customer.searchUsed++;
  return calculatePrice(product, customer);
}
```

### 3. حالات الطلب
```typescript
// الحالات الخارجية (يراها العميل)
type OrderStatus = 
  | 'PENDING'    // قيد الانتظار
  | 'APPROVED'   // تمت الموافقة
  | 'REJECTED'   // مرفوض
  | 'SHIPPED'    // تم الشحن
  | 'DELIVERED'  // تم التسليم
  | 'CANCELLED'; // ملغي

// الحالات الداخلية (للإدارة فقط)
type InternalStatus =
  | 'NEW'                    // جديد
  | 'SENT_TO_WAREHOUSE'      // أُرسل للمستودع
  | 'WAITING_PAYMENT'        // بانتظار الدفع
  | 'PAYMENT_CONFIRMED'      // تم تأكيد الدفع
  | 'SALES_INVOICE_CREATED'  // تم إنشاء الفاتورة
  | 'READY_FOR_SHIPMENT'     // جاهز للشحن
  | 'COMPLETED_INTERNAL';    // مكتمل داخلياً
```

---

## 🐛 المشاكل المعروفة والحلول

### 1. ملفات كبيرة جداً
**المشكلة:** `types.ts` و `mockApi.ts` كبيرة جداً
**الحل:** تقسيمها إلى ملفات أصغر (موجود في خطة التحسين)

### 2. استخدام `any` في بعض الأماكن
**المشكلة:** بعض الـ props تستخدم `any`
**الحل:** تحديد أنواع دقيقة تدريجياً

### 3. Mock API vs Real API
**المشكلة:** الاعتماد الكبير على Mock API
**الحل:** إكمال Real API endpoints

---

## 📝 مهام التطوير الشائعة

### إضافة صفحة جديدة
1. أنشئ ملف في `src/components/`
2. أضف Route في `App.tsx`
3. أضف ترجمات في `locales/*.json`

### إضافة API endpoint جديد
1. أنشئ route في `backend/src/routes/`
2. أنشئ controller في `backend/src/modules/`
3. أضف schema في `backend/src/schemas/`

### إضافة نوع جديد
1. أضف في `src/types.ts` (أو الملف المناسب)
2. حدّث Prisma schema إذا لزم الأمر
3. شغّل `npx prisma generate`

---

## 📞 للتواصل والأسئلة

إذا واجهت أي مشكلة:
1. راجع ملف `PROJECT_CONTEXT.md` للسياق العام
2. راجع ملف `docs/TECHNICAL_DOCUMENTATION.md` للتوثيق التقني
3. راجع ملف `docs/system_architecture.md` للهندسة المعمارية

---

## ✅ قائمة التحقق للمبرمج الجديد

- [ ] قرأت هذه الوثيقة بالكامل
- [ ] شغّلت المشروع محلياً
- [ ] جربت تسجيل الدخول كـ admin و customer
- [ ] فهمت هيكل الملفات
- [ ] فهمت تدفق الطلبات
- [ ] راجعت الملفات الكبيرة (types.ts, mockApi.ts)
- [ ] فهمت نظام الصلاحيات
- [ ] فهمت نظام التسعير

---

**آخر تحديث:** ديسمبر 2025
